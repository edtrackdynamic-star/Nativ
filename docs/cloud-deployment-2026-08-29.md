# פריסת ענן — 29 באוגוסט 2026

## משאבים חיים

- Firebase project: `edtrack-development`
- Web app: `Nativ Web` (`1:369491378125:web:ef7c802ad01f2da60b0e84`)
- Hosting site: `edtrack-nativ`
- כתובת: `https://edtrack-nativ.web.app`
- Firestore database: `nativ`, אזור `europe-west1`, מהדורת Standard, הגנת מחיקה פעילה
- Functions codebase: `nativ`, דור שני, Node.js 22, אזור `europe-west1`

## גבולות החיבור

- Firebase Auth משותף עם EdTrack; חוויית הכניסה והמיתוג נפרדות.
- EdTrack הוא מקור האמת לארגון, חברות ומנוי `nativ`.
- נתיב קוראת את רשומות הליבה בצד השרת בלבד ואינה כותבת לספריית EdTrack.
- תפקידי המוצר נשמרים ב־`organizations/{organizationId}/accessAssignments/{uid}` במסד `nativ`.
- תלמיד פעיל מקבל תפקיד תלמיד לפי חברות EdTrack. תפקידי צוות ניתנים רק כהקצאת מוצר מפורשת.
- מנהל הגישה הראשון יכול להפעיל את עצמו פעם אחת רק אם הוא `school_admin` פעיל ואין מנהל גישה אחר.
- פעולות שינוי דורשות מנוי מלא; בתקופת קריאה בלבד הן נחסמות.
- כל הגישה הישירה מהדפדפן למסד `nativ` חסומה ב־Rules.

## אימות שבוצע

- `pnpm check`: ‏32 בדיקות יחידה, lint, typecheck ו־build עברו.
- `pnpm test:emulator`: ‏6 בדיקות Firestore עברו.
- `pnpm test:system`: ‏4 תרחישי מערכת מלאים עברו מול Auth, Firestore ו־Functions Emulator.
- כללי Firestore והאינדקסים נפרסו למסד `nativ` בלבד.
- 23 callable Functions נפרסו תחת codebase `nativ`; פונקציות EdTrack לא שונו.
- Hosting נפרס בהצלחה והחזיר HTTP 200.
- קריאה חיה לפונקציה ללא הזדהות נדחתה ב־HTTP 401.

## חסמים לפני פתיחה תפעולית

1. להפעיל מנוי `nativ` עבור הארגון הרלוונטי במסך ניהול הפלטפורמה של EdTrack.
2. להתחבר עם מנהל ארגוני ולהפעיל את מנהל הגישה הראשון, ואז לאייש את רכזי השיבוץ ושאר התפקידים.
3. להחליט ולאשר במפורש את תוכן הדיוור והנמענים. הסוד `EDTRACK_SMTP_CONFIG` קיים ופעיל, אך לא חובר כדי לא להוציא מידע רגיש ללא אישור מדויק.
4. ליצור את הסוד `NATIV_GEMINI_API_KEY`. הסוד אינו קיים כרגע, ולכן הערכת AI אמיתית אינה מופעלת.
5. לרשום App Check לאתר, להוסיף את מפתח האתר ללקוח ורק לאחר ניטור להפעיל אכיפה בפונקציות.
6. להקים מחזור, מקבצים וקורסים אמיתיים או מסלול יבוא מאושר; הממשק עדיין מצביע על מזהה מחזור ההדגמה.

## חזרה לאחור

- ניתן להחזיר גרסת Hosting קודמת דרך היסטוריית Hosting.
- פריסות Functions עתידיות חייבות להשתמש ב־`--only functions:nativ`.
- פריסת כללים חייבת להשתמש ב־`--only firestore:nativ` כדי לא לגעת במסד ברירת המחדל של EdTrack.
- אין למחוק את מסד `nativ`; הגנת מחיקה הופעלה בעת יצירתו.
