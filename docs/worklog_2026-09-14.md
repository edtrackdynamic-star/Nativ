# יומן עבודה — 14.9.2026

## לוח מקבץ אחוד וצפייה בבחירות המקוריות

- חלונית התוצאות נפתחת כלוח במסך מלא של מקבץ אחד: עמודה לכל קורס, שם הקורס ומספר משובצים מתוך הקיבולת בכותרת, רשימת תלמידים עם כיתה ותג דירוג, ואזור נפרד למי שלא שובץ. דירוג ראשון, שני ושלישי ומטה מקבלים רקע חלש ונבדלים גם בטקסט; קורס שלא דורג נותר ניטרלי. מעבר מקבץ, חיפוש, תצוגות כיתה ותלמיד ויצוא Excel נשמרו.
- בחירת תלמיד ולאחריה כותרת קורס אחר מציגות תצוגה מקדימה בלבד: המיקום והספירות בלוח מתעדכנים ללא שמירה, ומופיעים קורס לפני/אחרי, דירוג חדש, קיבולת וסיבה לתיעוד. אפשר לבטל את ההצעה או לאשרה. לפני פרסום האישור יוצר גרסת שיבוץ מוצע חדשה; לאחר פרסום הוא מפעיל את מסלול השינוי המתועד ואת עדכון המזכירות הקיים. שינוי שמור אחרון ניתן לביטול רק כל עוד גרסת זרימת העבודה לא השתנתה; לאחר פרסום הביטול הוא שינוי נגדי מתועד עם עדכון נוסף, ולא מחיקה של ההיסטוריה. סגירה או מעבר תצוגה עם הצעה שלא אושרה מחייבים אישור.
- לחיצה כפולה על תלמיד או כפתור „בחירות” פותחים חלונית עם דירוגי המקבץ ונימוקו, וכפתור לפתיחת הטופס המקורי המלא בתוך אותה חלונית. הנתונים נטענים רק בעת הבקשה דרך `getStudentChoiceDetails`, המוגבלת בצד השרת ליכולת `nativ.assignment.manage`; רשימת כל התלמידים אינה כוללת נימוקים.
- מקור הדירוג המוצג בלוח הוא הבחירות המקוריות לקורס שבו התלמיד נמצא כעת, גם אם שיבוץ ישן שמר דירוג של קורס קודם. שינויים ידניים וערעורים חדשים שומרים גם במסמך השיבוץ את דירוג הקורס החדש. לא תוקנו בדיעבד מסמכי שיבוץ קיימים.

## חזרה לשלב העבודה האחרון

- האזור שנבחר, מחזור השיבוץ, דף הרכז והשלב בלוח התהליך נשמרים לפי מזהה המשתמש והארגון בדפדפן. בכניסה חוזרת משוחזרים רק תפקיד ומחזור שעדיין נגישים למשתמש; שלב שאינו מוכר נזנח. למשתמשים אחרים באותו דפדפן נשמר רצף נפרד. הטיוטות של תלמידים נטענות ממילא מהמערכת, ותלמיד שהשאיר טיוטה אחרי הגשה חוזר למסך עריכתה.
- זיכרון הניווט בדפדפן אינו מסתנכרן בין מכשירים. נשמרים בו מזהי מסכים ומחזור בלבד, ללא בחירות, נימוקים או שמות תלמידים. לא נוספו מסמכי Firestore או כללי גישה לצורך זה.

## אימות וגבול פריסה

- `pnpm check` עבר עם 129 בדיקות יחידה, lint, בדיקות טיפוסים ובניית Hosting. `pnpm test:system` עבר עם תשעה תרחישי מערכת, כולל שליפת הבחירות עם הרשאת רכז, דחיית תלמיד, בחירת גרסת הרצה עם בדיקת גרסה, ודירוג מעודכן אחרי ערעור ושינוי ידני.
- השינוי נשמר בקוד מקומי בלבד. לא נפרס ל־Hosting או ל־Functions, לא שונו Rules, ולא נגעו בנתוני שיבוץ חיים במסגרת המהלך הזה. לפני פריסה נדרש לפרוס את `getStudentChoiceDetails`, `selectAssignmentRun`, `changeStudentAssignment`, `executeAppealChange` ואת Hosting של נתיב כיחידה תואמת; פריסה של הממשק לבדו תשאיר מסלולים שבורים.

## פריסה מאושרת בהמשך היום

- לפני הפריסה אומת שהעותק המקומי נקי ושהענף המרוחק לא התקדם. `pnpm check` עבר שוב: lint, בדיקות טיפוסים, 129 בדיקות יחידה ובניית Hosting. גם `pnpm functions:build` עבר. תשעת תרחישי המערכת עברו במהלך המימוש הקודם; הם לא הורצו שוב בתור הפריסה.
- נפרסו ל־`edtrack-development` ארבע פעולות השרת בלבד ב־`nativ`, באזור `europe-west1`: `getStudentChoiceDetails`, `selectAssignmentRun`, `changeStudentAssignment`, `executeAppealChange`. הפקודה הסתיימה בהצלחה וכל הארבע הופיעו גם ברשימת הפעולות בענן.
- לאחר מכן נפרס `hosting:nativ` בלבד לאתר `edtrack-nativ`. בדיקת HTTP ל־`https://edtrack-nativ.web.app` ולקובץ ה־JavaScript החדש החזירה 200; ה־HTML החי מצביע על אותו קובץ שנוצר בבנייה המקומית, ובו נמצאים לוח המקבץ והקריאה ל־`getStudentChoiceDetails`.
- לא נפרסו Rules, לא בוצעו שינויי נתונים חיים ולא נבדק מסלול אינטראקטיבי מחובר עם משתמש אמיתי במסגרת הפריסה. הקוד נשאר כעת מקומי וטרם נדחף ל־GitHub.

## צמצום לוח השיבוץ וחריגת קיבולת ידנית

- לבקשת המשתמש לוח המקבץ קיבל שורת בקרה קומפקטית אחת: בחירת מקבץ, חיפוש, תפריט תצוגות וייצוא, ועזרה ומקרא נפתחים. הוסרו הכותרות וההוראות הכפולות מעל הלוח כדי להגדיל את גובהו הזמין. תצוגות כיתה, תלמיד וקורס וייצוא Excel נשמרו בתפריט.
- כאשר רכז בוחר תלמיד/ה ואז קורס שמלא לפי המקסימום הרגיל, השיבוץ הידני נשמר ללא טופס אישור נוסף וללא הקלדת נימוק. המערכת מציגה הודעה קטנה שנעלמת כעבור 4.5 שניות. היא שולחת לשרת כוונת חריגה מפורשת מתוך לחיצת הקורס, והשרת מחשב מחדש את הקיבולת בתוך העסקה, בודק גרסת זרימת עבודה והרשאת שיבוץ, ומתעד את זהות המבצע, הזמן והספירה לפני/אחרי. שינוי לאחר פרסום נשאר במסלול התיעוד ועדכון המזכירות הקיים. אפשרות ביטול השינוי האחרון נשמרה.
- תקרה קשיחה נפרדת (`capacity.limit`) עדיין חוסמת שיבוץ; גם הגבלות השתתפות, מדיניות חזרה שאוסרת וקיום ערעור פתוח לא נדרסות במסלול זה. חריגה מתועדת מהמקסימום הרגיל אינה מוצגת בטעות כנתון לא מוסבר שדורש בדיקה, אך המספר בפועל עדיין מוצג בכותרת הקורס.
- `pnpm check` עבר עם 133 בדיקות יחידה, lint, טיפוסים ובניית Hosting. `pnpm test:system` עבר עם תשעה תרחישים, כולל חסימת חריגה שלא צוינה במפורש ותיעוד חריגה לפני פרסום ואחריו. לא שונו Rules או נתוני שיבוץ חיים כחלק מהבדיקות.
- בהמשך אותו מהלך נפרסו ל־`edtrack-development` רק `functions:nativ:saveManualProposedAssignment`, `functions:nativ:changeStudentAssignment` ו־`hosting:nativ`. שתי הפעולות הופיעו ברשימת Functions לאחר הפריסה. האתר `https://edtrack-nativ.web.app` החזיר 200 והצביע לקובץ ה־JavaScript החדש `index-DBfAaaHM.js`, שהחזיר גם הוא 200 והכיל את חיווי החריגה ואת שורת הבקרה החדשה. לא נפרסו Rules ולא בוצעו שינויי שיבוץ חיים; לא בוצעה בדיקה אינטראקטיבית מחוברת לחשבון אמיתי.
