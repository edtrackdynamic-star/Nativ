# סיכום עבודה — 12 בספטמבר 2026

- במחזור בחירה חדש מוצג פתיח מוצע שניתן לעריכה. במחזור קיים הרכז יכול להחיל אותו בלחיצה יזומה. משפט העדיפות להסבר אישי ומנומק נשמר בנוסח שאושר.
- למחזור יש מסמך תקצירים אחד: קישור Google Docs או קובץ Word שמור. אותו מקור משמש לזיהוי תיאורי הקורסים ולפתיחת המסמך לרכז, למנחים המשויכים ולתלמידים כאשר הצגתו הופעלה. הצעות Gemini מוחלות רק לאחר בדיקת הרכז; תיאורים קיימים דורשים אישור החלפה.
- הרכז יכול לקבוע מועד סגירה אופציונלי לפי שעון ישראל, גם להאריך לאחר שפג. המועד מוצג לתלמיד ונאכף בשמירת טיוטה ובהגשה בצד השרת. תאריכים היסטוריים אינם נאכפים ללא סימון ההפעלה החדש.
- `pnpm check` עבר: lint, typecheck, בדיקות שרת, 88 בדיקות יחידה ובניית אתר. `pnpm test:system` עבר: שבע בדיקות מערכת מול Auth, Firestore, Functions ו-Storage Emulator.
- קומיט המימוש `f77590c` נדחף לענף `main` של Nativ. נפרסו `functions:nativ` ו-`hosting:nativ` בלבד; לא נפרסו Rules או יעדי EdTrack.
- אימות חי: `scripts/verify-release.mjs` אישר התאמה של שני קובצי האתר לבנייה, פונקציית Gemini פעילה עם קישור לסוד, ופונקציית רשימת התלמידים פעילה. ארבע הפונקציות הקשורות למסמך ולמועד מופיעות ברשימת הפונקציות החיות. הדף הציבורי של EdTrack מחזיר HTTP 200 ומכיל את כתובת נתיב הפעילה.
- בפריסה הראשונה לא בוצעה בדיקת ממשק עם חשבון רכז או תלמיד בסביבת הייצור, ולא שונו נתוני מחזור חי. מסמך Google פרטי עדיין דורש הרשאת צפייה לחשבון השירות, ולמורים ולתלמידים בהתאם להגדרות השיתוף ב-Google Drive.

## השלמת טופס התלמיד

- בטופס הבחירה ובתצוגה המקדימה מוצג כעת מיתוג בית הספר בראש הטופס עצמו: לוגו ייעודי כאשר נטען בהצלחה, ושם בית הספר כאשר אין לוגו. מקור המיתוג הוא אותה גישת ארגון המשמשת את כותרת נתיב; לא נוסף מקור נתונים מקביל.
- במחזור הטיוטה `cycle-fa3da772-1943-4e0d-b8a4-af69cbabb388` של `masa-learning-demo` הוחל דרך ממשק הרכז הפתיח המלא שאושר, והוסרה הוראה קצרה כפולה. השמירה אומתה בתצוגה המקדימה גם לאחר רענון. בחירות תלמידים לא שונו.
- לארגון ההדגמה הזה נתיב הלוגו המוגדר הוא `branding/product/logo.png`, שהוא לוגו מוצר כללי; קובץ לוגו בית ספר ב־`organizations/masa-learning-demo/branding/logo.png` אינו קיים. בהתאם לכלל המיתוג מוצג שם בית הספר בטופס.
- `pnpm check` עבר עם 90 בדיקות יחידה, lint, typecheck, בדיקות שרת ובניית ייצור. קומיט `3805084` נדחף ל־`main`; נפרס `hosting:nativ` בלבד. בדיקת השחרור אימתה שני קובצי אתר תואמים לבנייה ופונקציות פעילות, ובדיקת דפדפן מחוברת אישרה את הפתיח ואת שם בית הספר בטופס החי.

## הכנת הטופס וגישה למורים לפי קורס

- מסך „הגדרות וטופס” מרכז כעת את הגדרת מועד הסגירה ואת פתיחת הטופס לתלמידים. הכפתור „שליחת טופס לתלמידים” מופיע ליד השמירה, מאשר את הפעולה לפני פתיחה ומציג שגיאות תיקון ממוקדות; הפתיחה מציגה את הטופס לתלמידים הזכאים בנתיב ואינה שולחת דוא״ל. לוח „שיבוץ ותוצאות” נשאר לטיפול בהגשות, בשיבוץ ובתוצאות.
- תפקיד „מנחה קורס” נגזר משיוך המורה לקורס שנשמר בנתיב. אינדקס שיוכים מתעדכן בעסקת שמירת הקטלוג; גישה לקורסים ישנים ללא אינדקס נקראת מן הקטלוגים. הגישה מוגבלת לקורסי המורה, נשמרת כל עוד נותר לו קורס במחזור אחר, ואינה מפעילה משתמש מושעה. תפקידים אחרים נשארים בניהול גישה.
- `pnpm check` עבר עם 90 בדיקות יחידה ללא אזהרות lint; `pnpm test:system` עבר עם 8 בדיקות מערכת, כולל שיוך, הסרה, כמה מחזורים והשעיה. קומיט `0fae079` נדחף ל־`main`. נפרסו `functions:nativ` ו־`hosting:nativ` בלבד. `scripts/verify-release.mjs` אישר שני קובצי אתר תואמים ופונקציות פעילות; האתר הציבורי עדיין מפנה ל־`https://edtrack-nativ.web.app/`. בדיקת דפדפן מחוברת אישרה את הכפתור, מועד הסגירה והסרת הפתיחה הכפולה מלוח השיבוץ. מחזור ההדגמה נשאר בטיוטה ולא נשלח טופס לתלמידים.
