# סיכום עבודה — 12 בספטמבר 2026

- במחזור בחירה חדש מוצג פתיח מוצע שניתן לעריכה. במחזור קיים הרכז יכול להחיל אותו בלחיצה יזומה. משפט העדיפות להסבר אישי ומנומק נשמר בנוסח שאושר.
- למחזור יש מסמך תקצירים אחד: קישור Google Docs או קובץ Word שמור. אותו מקור משמש לזיהוי תיאורי הקורסים ולפתיחת המסמך לרכז, למנחים המשויכים ולתלמידים כאשר הצגתו הופעלה. הצעות Gemini מוחלות רק לאחר בדיקת הרכז; תיאורים קיימים דורשים אישור החלפה.
- הרכז יכול לקבוע מועד סגירה אופציונלי לפי שעון ישראל, גם להאריך לאחר שפג. המועד מוצג לתלמיד ונאכף בשמירת טיוטה ובהגשה בצד השרת. תאריכים היסטוריים אינם נאכפים ללא סימון ההפעלה החדש.
- `pnpm check` עבר: lint, typecheck, בדיקות שרת, 88 בדיקות יחידה ובניית אתר. `pnpm test:system` עבר: שבע בדיקות מערכת מול Auth, Firestore, Functions ו-Storage Emulator.
- קומיט המימוש `f77590c` נדחף לענף `main` של Nativ. נפרסו `functions:nativ` ו-`hosting:nativ` בלבד; לא נפרסו Rules או יעדי EdTrack.
- אימות חי: `scripts/verify-release.mjs` אישר התאמה של שני קובצי האתר לבנייה, פונקציית Gemini פעילה עם קישור לסוד, ופונקציית רשימת התלמידים פעילה. ארבע הפונקציות הקשורות למסמך ולמועד מופיעות ברשימת הפונקציות החיות. הדף הציבורי של EdTrack מחזיר HTTP 200 ומכיל את כתובת נתיב הפעילה.
- לא בוצעה בדיקת ממשק עם חשבון רכז או תלמיד בסביבת הייצור, ולא שונו נתוני מחזור חי. מסמך Google פרטי עדיין דורש הרשאת צפייה לחשבון השירות, ולמורים ולתלמידים בהתאם להגדרות השיתוף ב-Google Drive.
