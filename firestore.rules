rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // כל הגישה חסומה עד שמודל ההרשאות והפעולות בצד השרת יאושרו וייבדקו.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
